apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: mysql
data:
  MYSQL_ROOT_PASSWORD: cm9vdAo=  #base 64 encoded for "root"


# hum password direct nahi dalte hum isko base 64 karke dalte hai
#  echo "root" | base64
